scrapy crawl name 启动项目
extract()
extract_first()
response.css('title::text').extract_first()
response.css('title *::text').extract_first() 嵌套标签文字

response.urljoin(next_page)  // 相对路径变绝对路径

标签名::attr(属性名)
a::attr(href)
img::attr(src)


json转换
import json

 x = response.body
        y= json.loads(x)
        val = y['data']